[package]
name = "http2"
version = "1.0.0"
publish = false
edition.workspace = true

[dependencies]
actix-web = { version = "4", features = ["openssl"] }
openssl = { version = "0.10", features = ["v110"] }
