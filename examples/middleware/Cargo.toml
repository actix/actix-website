[package]
name = "middleware"
version = "1.0.0"
edition = "2018"

[dependencies]
actix-web = "3.3"
actix-session = "0.4"
futures = "0.3"
env_logger = "0.7"
# For Token auth middleware example
sqlx = {version = "0.5", features = ["runtime-actix-rustls", "sqlite"]}