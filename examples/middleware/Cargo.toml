[package]
name = "middleware"
version = "1.0.0"
edition = "2018"

[dependencies]
actix-web = "4"
actix-session = "0.5.0-beta.8"
futures-util = { version = "0.3.7", default-features = false, features = ["std"] }
env_logger = "0.7"
